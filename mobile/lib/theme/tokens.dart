// GENERATED FROM design/tokens.json — DO NOT EDIT. Run: python design/generate.py

import 'package:flutter/material.dart';

/// Design tokens shared with the web client. See docs/design-system.md.
abstract final class Tokens {
  // --- color ---
  static const ground = Color(0xFF0C1013);
  static const surface = Color(0xFF12181C);
  static const surfaceAlt = Color(0xFF171F24);
  static const line = Color(0xFF232C32);
  static const lineSoft = Color(0xFF1A2227);
  static const ink = Color(0xFFDCE3E6);
  static const inkDim = Color(0xFF83949C);
  static const inkFaint = Color(0xFF566670);
  static const signal = Color(0xFF4FD1C5);
  static const layerBrowser = Color(0xFF4FD1C5);
  static const layerEdge = Color(0xFF63B3ED);
  static const layerServer = Color(0xFFF2A65A);
  static const layerCloud = Color(0xFFE2725B);
  static const ok = Color(0xFF6EE7B7);
  static const warn = Color(0xFFFBBF24);
  static const danger = Color(0xFFF87171);

  // --- size (logical pixels; 1rem = 16) ---
  static const sNeg1 = 13.0;
  static const s0 = 16.0;
  static const s1 = 19.0;
  static const s2 = 24.0;
  static const s3 = 34.0;
  static const s4 = 48.0;

  // --- space (logical pixels) ---
  static const space1 = 4.0;
  static const space2 = 8.0;
  static const space3 = 12.0;
  static const space4 = 16.0;
  static const space5 = 24.0;
  static const space6 = 32.0;
  static const space7 = 48.0;
  static const space8 = 72.0;

  // --- motion ---
  static const fast = Duration(milliseconds: 120);
  static const base = Duration(milliseconds: 200);
  static const entrance = Duration(milliseconds: 760);
  static const stagger = Duration(milliseconds: 70);

  // --- radius ---
  static const sharp = 3.0;
}
