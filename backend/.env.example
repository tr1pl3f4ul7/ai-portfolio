# Shape reference for backend/.env — dummy values only, never the real thing.
# Copy to .env and fill in. .env is gitignored; this file is not.
#
# On the Oracle VM these come from the systemd unit's Environment= directives,
# and in CI from GitHub Actions secrets. There is no .env file in either place.
#
# Everything the backend can be configured with is listed in app/config.py, and
# everything except the key below has a working default. Only override one of
# those if you have a reason to.

# Required. Create at https://z.ai → API Keys.
# GLM-4.7-Flash is free, so this key has no spend attached to it — but it is
# still a credential, and it still never goes anywhere but here.
ZAI_API_KEY=REPLACE-ME

# Required for /contact. Create at https://resend.com/api-keys with "Sending
# access" only — it never needs to do anything but send.
RESEND_API_KEY=re_REPLACE_ME

# Where contact notifications land. With the default sender below, Resend will
# only deliver to the address the account was registered with.
CONTACT_NOTIFY_TO=you@example.com

# Error tracking. Leave unset locally — Sentry is off without it, which is what
# you want on a dev machine and in tests. Set it only on the VM. The DSN is
# write-only (it can send events, not read them). Create at
# https://sentry.io → your project → Settings → Client Keys (DSN).
#SENTRY_DSN=https://<key>@<org>.ingest.sentry.io/<project>

# Optional. Uncomment to override the defaults in app/config.py.
#AI_PORTFOLIO_ZAI_MODEL=glm-4.7-flash
#AI_PORTFOLIO_ZAI_BASE_URL=https://api.z.ai/api/paas/v4
#AI_PORTFOLIO_CONTACT_FROM=onboarding@resend.dev
#AI_PORTFOLIO_ENV=production
#AI_PORTFOLIO_SENTRY_TRACES=0.1
#AI_PORTFOLIO_CHAT_LIMIT_TOTAL=500
#AI_PORTFOLIO_CONTACT_LIMIT_TOTAL=50
